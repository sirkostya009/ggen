// Code generated by ggen. DO NOT EDIT.

import type { User as money_User } from "../shared/money";

export type Role = "admin" | "member";

/** Address is reached from both. */
export interface Address {
  city: string;
  zip: string;
}

/** User is a response. */
export interface User {
  id: number;
  role: Role;
  status: "active" | "banned" | "";
  plan?: 1 | 2;
  tags?: string[];
  created: string;
  level: string;
  point: Record<string, unknown>;
  money: money_User;
  address: Address;
  manager?: User;
  friends: (User | null)[] | null;
  state: number;
}

/** Tags is an annotated container alias. */
export type Tags = string[] | null;

/** Edges covers the remaining wire shapes. */
export type Edges = {
  list: string[] | null;
  max: number[] | null;
  plist: string[] | null;
  dict: Record<string, number> | null;
  quoted: string;
  unix: number;
  date: string;
  dur: string;
  hex: string | null;
  fixed: string;
  ip: string;
  addr: string;
  ns: string | null;
  raw: unknown;
  lower: "x" | "y" | "";
  nz: number;
  conv: number;
  tags: Tags;
} & Record<string, unknown>;
